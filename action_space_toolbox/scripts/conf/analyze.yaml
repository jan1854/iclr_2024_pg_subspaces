defaults:
  - _self_
  - analysis: gradient_analysis


train_logs: ??

min_interval: 200_000
first_checkpoint: 0
last_checkpoint: null
checkpoints_to_analyze: null
num_workers: 1
device: null
overwrite_results: false

log_dir: ${oc.env:ACTION_SPACE_TOOLBOX_LOG_PATH,logs}

# Disable the creation of the log directory
hydra:
  output_subdir: null
  run:
    dir: ${log_dir}/analysis/${now:%Y-%m-%d}/${now:%H-%M-%S}