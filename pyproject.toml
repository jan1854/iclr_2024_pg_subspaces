[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "action-space-toolbox"
description = ""
version = "0.0.1"
authors = [
    {name="Jan Schneider", email="Jan.Schneider@tuebingen.mpg.de"}
]
dependencies = [
    "dm_control",
    "dmc2gym @ git+https://github.com/denisyarats/dmc2gym.git",
    "gym[mujoco]==0.21",    # To ensure compatibility to stable-baselines3
    "numpy"
]

[project.optional-dependencies]
dev = ["pytest"]